<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.royaleburgers.kanishakanand.royaleburgers">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MenuActivity" />
        <activity android:name=".VegActivity" />
        <activity android:name=".NonVegActivity" />
        <activity android:name=".Pricelist_vegActivity" />
        <activity android:name=".pricelist_vegActivity" />
        <activity android:name=".pricelist_non_vegActivity" />
        <activity android:name=".paymentActivity" />
        <activity android:name=".thankyouActivity" />
        <activity android:name=".finalmapActivity"></activity>
    </application>

</manifest>